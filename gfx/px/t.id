// Test fixtures for gfx/ (internal): an 8x8 black surface with the 8x8 font
// loaded, and two ways to read the surface back. A case's setup and checks take
// no arguments, so the state they share has to be a function of its own.
idstd_sf_l_t_setup() {
  sf_l_init(8, 8);
  txt_g8_init();
} return void;
():() then idstd_sf_l_t_len:(64)
():() then idstd_sf_l_t_sum:(0)

idstd_sf_l_t_len() {
  int idstd_n = len((import sf_l_px));
} return int idstd_n;
given idstd_sf_l_t_setup ():(64)
given idstd_sf_l_t_setup ():(64) then idstd_sf_l_t_sum:(0)

idstd_sf_l_t_sum() {
  int idstd_m = lst_sum((import sf_l_px));
} return int idstd_m;
given idstd_sf_l_t_setup ():(0)
given idstd_sf_l_t_setup ():(0) then idstd_sf_l_t_len:(64)
