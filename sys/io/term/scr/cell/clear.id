// Every cell back to blank (a space, attribute 0) -- the start of each frame.
term_clear() {
  term_clear_from(0);
} return void;
given term_t_mark ():() then term_t_sum:(384)
given term_t_mark ():() then term_t_asum:(0)

term_clear_from(int i) {
  while(i < (import term_w) * (import term_h)) {
    term_blank_at(i);
    i = i + 1;
  }
} return void;
given term_t_mark (1):() then term_t_sum:(417)
given term_t_mark (0):() then term_t_asum:(0)

term_blank_at(int i) {
  lset((import term_scr), i, 32);
  lset((import term_attr), i, 0);
} return void;
given term_t_mark (0):() then term_t_sum:(384)
given term_t_mark (5):() then term_t_asum:(2)
