// A bounds-checked cell write -- what a game calls. An out-of-range list store
// aborts the process, so drawing past the edge has to be dropped here.
term_set(int idstd_x, int idstd_y, int idstd_c, int idstd_attr) {
  if(term_in(idstd_x, idstd_y)) {
    idstd_term_put(idstd_x, idstd_y, idstd_c, idstd_attr);
  }
} return void;
given idstd_term_t_setup (0, 0, 65, 2):() then idstd_term_t_sum:(417)
given idstd_term_t_setup (4, 0, 65, 2):() then idstd_term_t_sum:(384)

term_in(int idstd_x, int idstd_y) {
  int idstd_hit = 0;
  if(idstd_x >= 0 && idstd_x < (import term_w) && idstd_y >= 0 && idstd_y < (import term_h)) {
    idstd_hit = 1;
  }
} return int idstd_hit;
given idstd_term_t_setup (3, 2):(1)
given idstd_term_t_setup (0, 3):(0)

// One blank cell appended to both lists, while the buffer is being built.
idstd_term_blank() {
  push((import term_scr), 32);
  push((import term_attr), 0);
} return void;
given idstd_term_t_setup ():() then idstd_term_t_sum:(416)
given idstd_term_t_setup ():() then idstd_term_t_asum:(0)
