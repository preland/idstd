// The rest of a frame once the cursor is home: every row, then the finish.
idstd_term_render_body() {
  idstd_term_render_rows(0);
  idstd_term_finish();
} return void;
given idstd_term_t_setup ():() then idstd_term_t_sum:(384)
given idstd_term_t_mark ():() then idstd_term_t_asum:(2)

// Reset the colour and flush: a frame is built with put and flushed once.
idstd_term_finish() {
  string idstd_s = term_sgr(0);
  put(idstd_s);
  flush();
} return void;
given idstd_term_t_setup ():() then idstd_term_t_sum:(384)
given idstd_term_t_mark ():() then idstd_term_t_sum:(417)

// The escape that moves the cursor to the start of row y (rows count from 1).
idstd_term_rowpos(int idstd_y) {
  string idstd_s = chr(27) + "[" + (idstd_y + 1) + ";1H";
} return string idstd_s;
(0):("\033[1;1H")
(4):("\033[5;1H")
