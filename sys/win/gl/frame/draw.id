// Geometry, as milli-unit coordinates and packed colors. See gl.h.

native gl_draw_tris(int[] verts, int[] colors, int count) return int;

native gl_draw_points(int[] positions, int[] colors, int count, int size_x1000) return int;
